trigger:
 tags:
   include:
     - perf-test*
variables:
  ENVIRONMENT: dev
pool: cholpona

steps:
- script: |
    [[ "$BUILD_SOURCEBRANCH" == refs/tags/perf-test* && "$ENVIRONMENT" == "dev" ]] && NAMESPACE="perf-test" || echo "1"

